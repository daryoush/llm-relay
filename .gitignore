# clojure runtime config (may hold token; local only). The Python server
# no longer has any config file — settings are command-line arguments.
/config.json
server-clj/config.json

__pycache__/
*.pyc
.DS_Store
*.crswap
*.cpcache

# runtime outputs, at any level (home = wherever a server was launched)
instructions.md
debug_payload.jsonl

# clojure uberjar
server-clj/llm-relay.jar

# chat transcripts / scratch pads — local only
how-to-synch.md
