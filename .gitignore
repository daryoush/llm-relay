# local config may contain the shared token — never commit it
server/config.json

__pycache__/
*.pyc
.DS_Store
*.crswap

# default save target for received messages (remove this line to track it in git)
instructions.md

# clojure server may run with any launch directory as its home
/config.json
server-clj/llm-relay.jar

# raw request dump (diagnostics)
server/debug_payload.jsonl

*.cpcache

# chat transcripts / scratch pads — local only
how-to-synch.md
